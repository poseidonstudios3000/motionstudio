import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MOTN Studio — AI Talking Head Director",
  description:
    "Turn raw talking-head footage into premium 9:16 motion-graphic explainers with context-aware scenes, captions, and MP4 export.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/motion-studio-logo.png",
    shortcut: "/motion-studio-logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
